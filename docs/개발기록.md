# 개발기록 (DEVLOG) — FE3H v2.0

> **이 문서의 목적**: Claude Code 컨텍스트를 비워도 개발을 이어가기 위한 '살아있는 기록'.
> 새 세션은 CLAUDE.md → 이 문서를 읽고 현재 상태를 복원한다. **의미 있는 변경 후엔 이 문서를 갱신한다.**
> 최종 갱신: 2026-06-13 (참조 자료 '받아 적기' 정조준[인물 메모·견문록 가드] · 본문 우하단 '맨 아래로' FAB · 브랜치 정리[main + wizardly-dirac]. 직전: 본문 품질 대개편·Vercel Pro·소지품 그림 10종[497장])

---

## 1. 한눈에 — 지금 무엇이 돌아가나

루트에 v2.0 인터랙티브 픽션 앱이 서 있고, Vercel에 배포되어 실제로 동작한다.

핵심 고리:
```
[화면(React)] → [서버 한 겹] → [Claude Opus 4.8] → 본문 스트리밍 → [Supabase 저장] → 화면 복원
                   └ 주입: 박제 세계관(캐싱) + 큰 줄기·최근 화별 요약 + 활성 인물·문헌 + (지목 시)앵커
```

- **서버 한 겹**: 로컬은 Express(`server/index.mjs`), 배포는 Vercel 서버리스 함수(`api/*.mjs`). 둘이 같은 일.
- **원본 저장소 = Supabase**(클라우드). 브라우저는 Supabase에 직접 접근하지 않고 항상 서버(/api)를 거친다.
- **모델 분배**: 메인 본문 = Claude Opus 4.8(effort **'medium'**, adaptive thinking. 2026-06-13 사실 정밀도 실험으로 low→medium 승격 + Vercel Pro로 60초 천장 해제, 끊김 재발 시 low 회귀). **Gemini(gemini-2.5-flash) = 화별 요약·큰 줄기·앵커 추림·인물 분석 보고서에 사용 중**(`lib/summarize.mjs`·`lib/report.mjs`). 미연결: 편지 7종·인물 추출·해시태그.

---

## 2. 기술 스택 / 운영

- Vite + React 19 + TypeScript. (Zustand 미도입 — 아직 상태가 단순)
- 로컬 개발: `npm run dev` (vite 5173 + express 8787, vite가 `/api`를 8787로 프록시).
- 빌드: `npm run build` (= `npm run bake && vite build`).
- **세계관 굽기**: `npm run bake` → `scripts/bake-worldview.mjs`가 `worldview/` 박제 4종을 `lib/worldview.mjs`로 구워 넣음(dev·build 시 자동). **worldview/ 고치면 재배포해야 반영.** (옛 `md/` 폴더는 worldview/로 개칭; 안 구워지던 참고 템플릿 3종은 삭제 — 인물·견문록은 유저 커스텀 DB로만 관리.)
- **배포 = `main`에 push** → Vercel 자동 배포(프로덕션 도메인 `crest-if.vercel.app`). 개발 브랜치 `claude/wizardly-dirac-WjCg3`에서 작업 → `main`에 머지 → push.
- 배포 전 항상 `npm run lint`(tsc) + `npm run build` 통과 확인.
- ⚠️ **Vercel 자동배포가 불안정한 진짜 원인(중요)**: Claude Code 웹 세션은 **클라우드 샌드박스 → git 프록시 → GitHub** 경로로 push한다. 커밋은 GitHub에 **항상** 도달하지만(매번 MCP로 확인됨), 이 경로의 push는 GitHub→Vercel **자동 웹훅을 들쭉날쭉하게만** 깨운다(어떤 푸시는 배포, 어떤 푸시는 무시). **한도/권한 문제 아님**(Usage 여유, 권한 정상). GitHub이 **직접** 내는 push(웹 편집·본인 머신)는 항상 정상 트리거됨.
- ⚠️ **Deploy Hook도 막다른 길이었음**: GitHub Action이 Deploy Hook을 정상 호출(초록 성공)해도 Vercel이 배포를 안 만듦(이 프로젝트의 git연동/Hook 처리 자체가 불안정). 즉 Vercel의 트리거 수신부를 의존하는 방식은 전부 실패.
- ✅ **최종 해결 = GitHub Action에서 Vercel CLI로 직접 빌드·업로드**: `.github/workflows/deploy.yml` — `on: push(main)` + `workflow_dispatch`. GitHub 러너가 `vercel pull`→`vercel build --prod`→`vercel deploy --prebuilt --prod` 로 **결과물을 직접 프로덕션에 올림**(Vercel git연동/Hook 안 거침 → 그쪽 불안정과 무관). Action 트리거는 샌드박스 push에도 안정적(실측 3/3 실행 확인). **비밀값**: `VERCEL_TOKEN`(Vercel Settings→Tokens), `VERCEL_ORG_ID`(Settings→General). `VERCEL_PROJECT_ID`(=`prj_pO0MqjnDbLnnOH7coic2GnjiU7FT`)는 비밀 아니라 워크플로 env에 박음. 자동 누락 시 **Actions→Run workflow** 수동.
- 옛 `VERCEL_DEPLOY_HOOK` 시크릿은 미사용(삭제 가능). 코드는 평소대로 `main` 머지+push만 하면 Action이 빌드·배포까지 끝냄.
- ✅ **(2026-06-09) Vercel Git 연동 '해제'(Disconnect) — 근본 정리**: 그동안 push마다 **①Action의 CLI 배포(`>_`) + ②Vercel Git 자동배포(`-o-`)가 둘 다** 생성돼 Hobby 빌드 큐가 막혀(프로덕션 "Queued"에서 안 빠짐) 배포가 30분+ 멈추는 사고가 났음. 원인=Git 연동이 안 꺼져 있어 이중 생성. **해결=Vercel Settings→Git→Disconnect**(프로젝트는 그대로, `VERCEL_TOKEN`/`ORG_ID`/`PROJECT_ID`로 Action이 배포하므로 무관). 이제 **Action(CLI) 단독 → 중복·큐잼 없음**. ⚠️ 막혔을 때 MCP 토큰엔 Actions 취소/dispatch 권한 없음(403) → 빈 커밋 재push 또는 빌더가 Actions UI에서 수동.

### 환경 변수 (Vercel 설정 + 로컬 `.env`)
- `ANTHROPIC_API_KEY` — 클로드 본문. **(필수)**
- `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` — 저장소. **service_role(비밀) 사용. anon 아님.** (없으면 저장 없이 동작)
- `GEMINI_API_KEY` — 설정만 됨, 아직 쓰는 코드 없음.
- ⚠️ Vite는 `VITE_` 접두사 변수를 브라우저에 노출 → 비밀 키에 절대 `VITE_` 붙이지 말 것.

### Supabase 표 (SQL Editor에서 1회 실행, RLS on·정책 없음 → service_role만 접근)
- `stories(id, title, created_at, updated_at)` — 세이브 슬롯(이야기).
- `turns(id, role, content, story_id→stories on delete cascade, created_at)` — 본문(이야기별).
- `characters(..., name, english_name, aliases, base, gender, faction, rank, crest, appearance(미사용), height, build, hair, iris, impression, personality, combat, notes, bonds(jsonb), life_status, is_active, thumbnail, avatar, sort_order, title(미사용), story_id→stories cascade, created_at)` — **이야기별 분리**. `thumbnail`=초상(뷰 히어로용), `avatar`=얼굴(명부 목록 둥근 썸네일·없으면 thumbnail). 약력 항목: 신원(성별/소속/신분/문장/상태), 용모(신장·체격·모발·홍채·인상), 성향·전법·비고(마크다운), 인연(bonds). `appearance`·`title`은 옛 필드(미사용·DB 잔존).
- `lore(id, title, category, body, sections, is_active, sort_order, story_id→stories cascade, created_at)` — 견문록(=대륙 문헌), **이야기별 분리**. `category`=5영역 중 하나(드릴다운 분류), `sections`(jsonb)=다중 소주제 `[{subtitle,content}]`(진짜 출처), `body`=섹션의 평문 거울(주입·앵커가 읽음·프런트가 동기화).
  - ⚠️ **소주제 컬럼 (다중 소주제 — 1회 실행. 없으면 그 구조만 저장 안 되고 body 평문으로 폴백·무손실)**: `alter table lore add column if not exists sections jsonb;`
- ⚠️ **요약/연대 문헌용 추가 컬럼 (실기 전 1회 실행 필요 — 빌더가 아직 안 돌렸으면 폴백 모드로 동작)**:
  `alter table turns add column if not exists summary text;`
  `alter table stories add column if not exists summary text;`  (큰 줄기)
  `alter table stories add column if not exists summary_turn_id bigint;`  (흡수 포인터)
  (셋 다 없어도 안 깨짐 → '전체 원문' 폴백. 넣어야 화별 요약·연대 문헌·큰 줄기 작동.)
- ⚠️ **인물 명부 확장 컬럼 (약력 개편 — 각 1회 실행. 없으면 그 항목만 저장 안 됨)**:
  `alter table characters add column if not exists avatar text;` (목록 얼굴)
  `alter table characters add column if not exists rank text;` (신분)
  `alter table characters add column if not exists crest text;` (문장)
  `alter table characters add column if not exists gender text;` (성별)
  `alter table characters add column if not exists base text;` (거점)
  `alter table characters add column if not exists height text;` `... build text;` `... hair text;` `... iris text;` `... impression text;` (용모 5항목)
  `alter table characters add column if not exists bonds jsonb;` (인연 배열)
  `alter table characters add column if not exists analysis jsonb;` (분석 보고서 — 보고서 탭. 없으면 발급 시 "보고서 담을 자리 없음" 안내로 폴백)
  (※ 서버 코드(db.mjs 인물칸)도 바뀌므로 **로컬은 SQL 실행 후 dev 서버 재시작** 필요. 프로덕션은 같은 Supabase라 SQL 1회면 공유됨.)

### 진단
- `/api/turns`, `/api/characters`, `/api/lore`를 브라우저로 열면 `{dbReady, error, ...}`가 보임.
  - `dbReady:false` → 키 미설정/이름오타/미배포. `error`에 "relation ... does not exist" → 표 없음. "row-level security/permission" → anon 키 잘못 사용.

---

## 3. 데이터 분류 (박제 / 커스텀) — 핵심 설계

| 층 | 내용 | 처리 |
|---|---|---|
| **박제** (원작 정사·작법) | 세계관 규약 · 집필 지침 · 성인향 묘사 지침 · 고유명사 사전 | `worldview/` → bake → 시스템 프롬프트로 **항상 주입 + 프롬프트 캐싱** |
| **커스텀 — 인물** | 인물 설정 사전 · 주요 인물 노트 | **인물 기능**(characters 표 + UI) |
| **커스텀 — 세계** | 연재 고유 설정 | **견문록 기능**(lore 표 + UI) |

- 새 고유명사는 고유명사 사전(박제)을 건드리지 않고 커스텀(인물/견문록)에 추가한다.

---

## 4. 빌드된 기능 + 파일 지도

- **이야기 생성·저장·복원**: `api/story.mjs`·`server`(스트리밍, turns 저장), `src/App.tsx`(켤 때 `/api/turns?story_id=`로 복원).
- **세이브 슬롯(이야기 관리)**: `stories` 표 + `api/stories.mjs`·`server`(목록/생성/이름변경/삭제). `src/Stories.tsx`(헤더 '이야기' 버튼) + App의 부팅 로직(현재 이야기 = localStorage `fe3h.currentStoryId`, 없으면 '이야기 1' 자동 생성). 본문 생성/저장은 현재 `story_id`로. 삭제 시 해당 본문 cascade. **인물·견문록도 이야기별 분리** — 각 패널의 "다른 이야기에서 불러오기"(`src/ImportDialog.tsx`)로 독립 복사. 인물·견문록 캐시/조회/주입 모두 `story_id` 스코프.
- **세계관 주입**: `lib/worldview.mjs`(박제, 캐시 블록) + `lib/charContext.mjs`(활성 인물) + `lib/loreContext.mjs`(활성 견문록). 인물·견문록 블록은 **박제 뒤에 붙여 캐싱 유지**.
  - 인물 주입 정책 = **B(핵심+참조라벨)**: 이름·이명·소속·정의·성격은 늘, 외양·무기·비고는 '참조용 — 필요할 때만' 라벨.
  - 견문록 주입: '원작 기본값보다 우선', '설명조 나열 금지'.
  - ⚠️ **'받아 적기' 가드(2026-06-13)**: 인물·견문록 둘 다 **참조 자료를 본문으로 베끼는** 사고가 잦다(인상 "여신 같은 미모" 직출 / 구조도 문장 paraphrase). 두 주입 블록에 각각 "설정값은 메모지 본문 아님 — 그대로든 말만 바꿔서든 옮기지 말고, 사실은 인물 행동·시선에 간접적으로만 비치게" 명시. 구조·정의문 말투("A는 B로 구성된다/들어설 자리")는 베낀 티로 지목. **서버 런타임(bake 무관), 로컬은 dev 재시작 반영.**
- **인물 명부**(메뉴 '인물 명부'): `src/Characters.tsx`(대형) + `src/imageUtils.ts`(업로드→WebP·투명) + `src/FaceCrop.tsx`(초점 지정 크롭) + `src/Dropdown.tsx`(커스텀 드롭다운) + `src/Markdown.tsx`(경량 MD) + `src/nameUtils.ts`(firstName·splitAliases) + `src/nameDict.generated.ts`(bake가 고유명사 사전→{한글:영문}) + `lib/db.mjs` CRUD. **모달은 항상 `has-hero`** = 데스크탑 760(목록 모드는 `modal--list` 620)·**모바일 풀스크린**(`.modal-bg:has(.modal.has-hero)` 여백0, 안전영역 패딩). `.modal-bg{overscroll-behavior:none}`로 스크롤 바운스 차단. **목록 = 초상 갤러리**(`char-grid`+`SortableCharCard`, 카드 3:4·이름 퍼스트네임). 공용 자산: `IconButton`(둥근 상단 버튼)·`ListRow`(배너 행)·`Spinner`(가운데 로딩)·`Logo`·`src/strings.ts`(UI 라벨).
  - **목록**: 둥근 얼굴 썸네일(테두리 링)+성명+첫 이명. 행 우측 **북마크=활성(등장) 토글**(즉시 저장), 잠든 인물 흐림. 행 **드래그 정렬**(dnd-kit, `sort_order` 저장). 상단 버튼 `＋ 명부 추가`/`↧ 명부 반입`. 생사: 사망=흑백, 불명=흑백+어둡게+`?`(목록·히어로 공통, `statusFx`).
  - **뷰**: **히어로**(좌 초상+우 정보) — 성명(`clamp(12px,7cqi,23px)`·nowrap, `container-type:inline-size`로 한 줄 보장)·영문(Hanken Grotesk)·이명 칩(10px)·거점(노란 MapPin 12px). 상단 아이콘 버튼 **[북마크][편집][닫기]**(+←). 아래 **탭(약력/보고서/임무/소지품/서신)** — 약력·보고서·임무 구현(소지품·서신은 빈 탭). 탭 `overflow:hidden`(스크롤 없음).
  - **소지품 그림 시스템**(2026-06-10~12 완성, emoji 대체): `lib/itemIcons.mjs`가 보유 그림 단일 출처 — **`ITEM_ICON_GROUPS`(11분류 구조) + 파생 `ITEM_ICONS`/`ICON_BY_LABEL`/`ICON_KEYS` + `ICON_ALIASES`(동의어)**. 최종 **487장(폴백 1 + 11분류 486종)**: 문서37·장신구69·생활115·의복71·무기36·종교14·증표21·약초꽃27·자연물16·음식식재료73·마물7.
    - **매칭 파이프라인**: AI가 탐색 시 [그림 후보](분류 머리글로 묶음 — "분류 먼저 → 그 안에서 이름·재질·용도가 가장 구체적으로 일치하는 라벨" 2단 규칙, 호출은 1번)에서 골라 `icon` 키로 박제. 환원 순서 = 라벨 → key → **별칭(ICON_ALIASES)** → 실패 시 폴백 + **분실물 대장 로그**(`[소지품] 분실물 대장` — 서버 콘솔, 새는 표기를 보고 별칭을 보탠다). 그림 priming 차단(후보 목록은 매칭 전용 — 물건 창작에 못 새게 규칙 명시).
    - **화면(`ItemIcon`)**: `/assets/illust/items/<key>.webp`, 실패 시 fallback.webp(404 안전). 아이콘은 오직 icon prop의 함수 — 실패 상태를 '그 아이콘 이름'에 묶고 `<img key>`로 잔상 차단(이웃 칸 소각 시 폴백 전염 버그 수정됨).
    - ⚠️ 명단·문서 정리는 **분류별**(업로드 회차 묶음 금지). 갱신 시 명단·문서를 같은 데이터에서 동시 재생성해 카운트 정합 보장. 명세·파일명·생성 프롬프트 = `docs/에셋제작_소지품.md`, 그림 폴더 `public/assets/illust/items/`. **아이템 콘텐츠는 이로써 일단락.**
  - **소지품 탭**(2026-06-10, legacy 인벤토리 이식·emoji 제외): `lib/items.mjs` + `api/items.mjs`·`server`(POST=탐색 3점 누적 / DELETE=개별 소각) + `Characters.tsx ItemsView`. **탐색마다 기존 목록에 누적**(주머니가 차는 수집 감각 — 빌더 결정), 텍스트 카드만(이름 15자 + 25자 한 문장). 설명 화법 = 추론·감각·상태만, **출처 단정 금지**(§1 '각자의 시선'과 동결). 영웅의 유물·게임 보구 금지(시대 방화벽), 기존 물건과 중복 금지, NAMES 풍미는 임무와 같은 예외. 저장 = `analysis.belongings`(+`belongings_at`), 각 물건에 서버 발급 안정 id(소각용). 보고서 재작성의 보존 merge가 quests+belongings 둘 다 지킴. 카드 소각 = `confirmAsk` 경유, `UI.explore`('탐색') 등재.
  - **임무 탭**(2026-06-10, legacy 퀘스트 이식): `lib/quests.mjs` + `api/quests.mjs`·`server`(POST {character_id, story_id}) + `Characters.tsx QuestsView`. Gemini Flash가 '인물이 스스로 꾸미는 계획' 5~7건(유형 의무·야망·교류·휴식·돌발 / 제목 명사형 15자 / 1인칭 독백 70자 / 보상='행위 뒤에 남는 잔여물')을 JSON 발급. **보고서와 따로 발급·재작성**(구경거리 전용, 본문 연동 없음 — 빌더 결정). 저장 = `characters.analysis.quests`(+`quests_at`, 스키마 변경 없음) — 보고서 재작성 시 quests 보존(`runReport` merge). 프롬프트는 v2 결로 재작성(few-shot 없이 규칙·금지), 정합성 가드는 빌더 결정으로 생략. **고유명사 '표기 목록'(`NAMES`) 주입은 §3 승인 예외** — bake가 `lib/worldview.mjs`에 한글 명단 export. 맥락·약력·JSON 헬퍼는 `report.mjs`에서 재사용(export).
  - **약력**: `신원|용모` **2열 카드 그리드**(`info-grid` auto-fit) — 신원=성별/소속/신분/문장/상태(InfoRow), 용모=신장/체격/모발/홍채/인상. + `성향/전법/비고` **글 카드**(`ViewSection`, **마크다운**: `- `=회색 박스 불릿). + **인연**(접이식 카드, 기본 접힘, grid-rows 애니메이션): 아바타(명부 이름 자동매칭)+퍼스트네임+관계(부제)+설명, **뷰에서 드래그 정렬**(즉시 저장). 카드 제목 14px.
  - **편집**(="명부 편집", 뷰와 동일 레이아웃): 히어로에 초상+성명/영문/이명/거점 입력(밑줄형, 데스크탑은 라벨 한 줄), 초상 위 **아이콘 툴바**(초상 등록/초점 지정/초점 해제/초상 소각·hover 툴팁). 신원|용모 카드(입력), 성향/전법/비고 textarea, 인연 추가/삭제. **성명 입력 시 영문명 자동 매칭**(nameDict, onBlur). 드롭다운(성별/상태)은 커스텀. 저장 버튼은 크기 고정+스피너(텍스트 변동 X). 활성은 편집 아닌 **뷰 북마크**로.
  - **기본 그림**: 히어로=`/avatar-placeholder.webp` · 목록=`/portrait-placeholder.webp`(사용자 지정 매핑). `appearance`(옛 외양 자유서술)·`title`(옛 한줄정의)은 미사용.
  - ⚠️ 인물 많아지면 초상(base64 inline)→Supabase Storage 이전 검토.
- **대륙 문헌**(메뉴 '대륙 문헌', 개별=문헌): `src/Lore.tsx` + `lib/db.mjs` CRUD. **5영역 드릴다운 + 뷰/편찬 분리 + 다중 소주제** 완료.
  - **첫 화면 = 5영역 배너**(`TOPICS`: 세력·가문/지역·지리/역사·사건/제도·문화/문장·유산, `list-row` 배너형·일러스트 `public/assets/illust/lore-*.webp`·우측 권수). 영역=`category` 칸. 5개와 안 맞는 옛 분류는 **'미분류'** 칸으로 모아 재분류(무손실).
  - **갈래 화면**=그 영역의 문헌 목록(제N권=전체 순서·앵커 정합 유지). 행 우측 **북마크=활성(반영) 토글**(즉시 저장), 잠든 문헌 흐림(인물 명부와 동일·'잠듦' 태그는 제거).
  - **뷰**: 영역 칩+제N권, **소주제 목차 한 줄**(소제목 ` · `), 소주제별 본문(강조바 제목+Markdown). 상단 **[북마크][편찬][닫기]**(+←). 옛 문헌(섹션 없음)은 body로 폴백 렌더.
  - **편찬**: 영역(드롭다운·좌 25%, 모바일은 세로)·갈래(이름·우)·**소주제 칸 여러 개**(드래그 정렬=`SortableSec`/dnd-kit, 손잡이 그립만·텍스트 선택 방해 X)+`＋소주제 추가`. 저장 시 `sections`(진짜 출처)+`body`(평문 거울 `sectionsToText`) 동시 기록. 활성은 뷰 북마크로(편찬엔 체크박스 없음).
  - 모달은 **`has-hero modal--lore`** = 데스크탑 620·모바일 풀스크린(인물 명부와 동일). 행위어 '편찬'=`UI.compile`.
  - ⚠️ legacy 견문록 대비 **남은 것: 검색·AI 문장 정제·항목(권) 드래그 정렬·이전/다음 권 네비**(다음 후보).
- **메뉴(드로어)**: 인물 명부 · 대륙 문헌 · 연대 문헌 · 기록 지침 · 천각의 박동 (`src/App.tsx` menuItems). 연대 문헌=`src/Chronicle.tsx`.
- **연대 문헌 상호작용(2026-06-09)**: 평평한 목록 → **목록(2줄 미리보기) → 항목 클릭 시 상세 뷰**(제목+요약 전문) + 상세에서 **'그 화로 가기'**(목차 점프). 점프=App `jumpToTurn(turnId)`: `turns`에서 그 id 찾아 `visibleCount`를 넓혀(윈도잉) 그 화를 렌더 → `pendingJump` effect가 `[data-turn-id]`로 스크롤(`scroll-margin-top`로 헤더 안 가림). 목록 행은 `<button.chronicle-row>`(클릭), 요약 미리보기 `.chronicle-summary.clamp2`(2줄). 상세=`.chronicle-view`(`BookOpen` 가기 버튼). `Chronicle`에 `onJump` prop 추가.
- **기록 지침**(메뉴 '기록 지침', 연대 문헌 아래): 기록자(유저)가 직접 적어 **모든 이야기의 본문에 공통 주입**하는 전역 커스텀 집필 지침. 박제 worldview는 못 건드리지만 그 위에 실시간으로 얹는 개인 지침서(예: "수치(키·나이) 본문 노출 금지"). 저장 즉시 다음 본문부터 반영(배포·bake 불필요). 파일: `src/Guidance.tsx`(글칸+기록) + `api/guidance.mjs`·서버 `GET/POST /api/guidance` + `lib/guidance.mjs`(`buildGuidanceBlock`) + `lib/db.mjs`(`getGuidance`·`setGuidance`, settings 표). **주입 위치**: 박제 SYSTEM **바로 뒤**(캐시 경계 밖이라 캐싱 안 깸), story·regen 양쪽. **메인 본문 전용** — Gemini(요약·보고서)엔 안 넣음(§3). ⚠️ **스키마**: `create table if not exists settings (key text primary key, value text, updated_at timestamptz default now());` (없으면 주입 0·저장 실패로 안전 폴백).
- **목록 캐시**: `src/useCachedList.ts`(stale-while-revalidate: 메모리 + IndexedDB) → `useCharacters`·`useLore`가 얇은 래퍼. `src/idbCache.ts`(의존성 없는 IndexedDB KV). **원본은 Supabase, 캐시는 빠른 사본.**
- **폰트**: 국문 SUIT(jsDelivr CDN @font-face), 영문 Hanken Grotesk(Google Fonts), 서명·명문 Pinyon Script.
- **본문 표현**: `src/StoryText.tsx`(react-markdown + remark-gfm)로 서술자 본문을 마크다운 렌더. 라틴(=포드라 문자)은 `src/podraScript.ts`의 rehype 플러그인이 `.script`(Pinyon)로 감쌈. 결과값 '복사' 버튼은 `stripMarkdown`으로 평문 복사.
- **테마**: 다크(메인 `#2d2d3a` + 양피지 금빛 강조). 색은 `src/styles.css`의 `:root` CSS 변수에서 한 곳 관리.
- **메인 화면(읽기/집필 모드)**: 헤더 토글(localStorage `fe3h.mode`). 읽기 모드 = 프롬프트·입력창·액션 숨기고 본문만(소설처럼 크게). 디제틱: 장면 구분 `⁂`(rehype가 hr→`.scene-break`), 회차 제목 가운데, 은은한 비네팅. 프롬프트는 우측 칩(글자는 좌측), 액션 아이콘도 우측.
- **모바일 웹앱 마감(중요)**: ① **안전영역**: `viewport-fit=cover` + `.head`/`.compose`/`.drawer`/`.modal-bg`/헤더버튼 `top`에 `env(safe-area-inset-*)`(다이나믹 아일랜드·홈 인디케이터 회피). 입력바 하단은 `max(8px, inset-16px)`로 바짝. ② **포커스 줌 방지**: viewport `maximum-scale=1, user-scalable=no`(입력 15px라 iOS 자동확대 → 잠금). ③ **키보드 높이 버그**: 100dvh가 iOS 웹앱에서 키보드에 흔들려 → `main.tsx`가 **VisualViewport**로 `--app-height`/`--app-top` 실측, `.page { position: fixed; top: var(--app-top); height: var(--app-height) }`로 보이는 뷰포트에 고정.
- **사이트 껍데기**: `index.html` title=**Fire Emblem: Three Houses**, OG/Twitter(절대주소 crest-if.vercel.app)·favicon·apple-touch-icon·`site.webmanifest`·theme-color. 정적 파일은 **`public/`** — **루트=외부 자산**(favicon들·apple-touch-icon·icon-192/512·og-image·site.webmanifest, SNS·OS가 고정 주소로 요청), **`public/assets/`=내부 자산**(`illust/`=일러스트·플레이스홀더, `icon/`=파일형 아이콘). 코드 참조는 `/assets/illust/…`(Characters의 HERO/LIST_PLACEHOLDER). 빌드 시 dist로 그대로 복사. 큰 캐릭터 에셋은 Supabase Storage(§에셋 원칙).
- **메인 레이아웃·입력**: `.page` 셸(이젠 `position:fixed`+VisualViewport, 위 모바일 마감 참고) + `body{overflow:hidden}`로 헤더·입력바 고정. `.scroll`은 전체 폭(스크롤바 창 끝), 본문은 `.scroll-inner` 720 가운데. 헤더 버튼(MenuIcon/모드토글)은 창 양끝, 제목 가운데. 입력칸 자동높이(1줄 시작→`composeRef` 측정), 버튼 라벨 '전개'. 엔터: 데스크탑 전송/Shift+Enter 줄바꿈, 모바일(coarse 포인터) 줄바꿈, 한글 조합 중 무시. 메시지마다 복사/새로받기/수정/삭제(lucide 아이콘, hover). 스크롤바 전부 얇게, 포커스 파란 아웃라인 제거(금빛 테두리). ⚠️ lucide `Menu`는 드로어 `Menu`와 충돌하니 `MenuIcon`으로.

---

## 5. 주요 결정과 이유 (왜 이렇게 했나)

- **md(문서) vs .ts(코드)**: 세계관을 .md로 두는 건 클로드 입장에선 동일(텍스트만 봄). 단지 비개발자가 편집하기 쉽고, 콘텐츠(유저 영역)와 코드(시스템) 분리. 코드가 직접 import 못 하니 bake로 다리를 놓음.
- **Supabase = 창고(DB)**, Vercel = 앱·서버. Supabase를 '우리 서버'로 쓰지 않고 서버 뒤 창고로 둠(키 숨김).
- **브라우저 캐시 = IndexedDB**(localStorage 5MB·동기 한계 회피). 단 원본은 Supabase, 캐시는 임시 사본.
- **썸네일 = WebP**(wbmp 아님 — wbmp는 흑백 1비트 구식). 600px 리사이즈. DB 인라인 저장(소량이라 Storage 버킷 생략).
- **인물 주입은 '전부'가 아니라 핵심+참조라벨**: 다 넣으면 모델이 외양·무기를 습관적으로 읊어 글이 산만 → 집필 지침의 "아는 걸 다 꺼내지 말 것"과 충돌.
- **대륙 문헌 다섯 영역 = 세력축**(2026-06-10): 주제축(세력·가문/지역·지리/역사·사건/제도·문화/문장·유산) → **제국 / 왕국 / 동맹 / 교단 / 만상(萬象)**. 빌더의 분류 직관이 세력 기준이라 교체. '만상' = 어느 세력에도 안 속하는 대륙 안팎·공통 기록(기타 아님, 당당한 다섯째 서랍). 수정 지점은 `Lore.tsx TOPICS` + `lib/anchor.mjs 영역들` 두 곳(일치 유지). 기존 문헌은 Supabase SQL로 일괄 '왕국' 이동(7권 시점). 옛 영역명 앵커("세력 제1권")는 더 안 읽히나 제목 앵커는 유효. ⚠️ 입구 일러스트 5장은 옛 주제 그림을 임시 매핑 — 새 그림 반입 시 교체.

---

## 6. 알려진 한계 · 다음 할 일

### ▶ 이번 주말 작업 요약 (2026-06-07, 토~일)
한 묶음으로 작업 후 `main` ff+push 1회로 배포. 주요 변경:
- **보고서 탭(신규·완료)** — Gemini Flash 자동 발급 분석 보고서. 능력치 8종 **레이더(팔각형 SVG)+막대**, 성격/무의식, 평판 6종(외부 인연=상호성), 인용구·해시태그. **2단 레이아웃**(좌 능력치/우 분석). 미발급/발급중엔 골격 흐림+발급 버튼. 맥락=본문과 같은 3겹(읽기 전용, **작가 지시 제외**·서술자 본문만). thinking 켜되 예산 한정(2048)+maxOutputTokens 10000(잘림 방지). 파일: `lib/report.mjs`·`api/report.mjs`·서버 라우트·`db.mjs`(`analysis` jsonb).
- **UI 언어 전수 정비** — 해요체 전부 →하십시오체/~니다, 디제틱 사전 위반 청소(복사 제목 버그 등). **`src/strings.ts`(UI 라벨 단일 출처)** 도입 → 시스템어 타이핑 불가. CLAUDE.md §1에 말투 규칙+체크리스트 박음. ("다시 받기"→**재작성**)
- **공용 자산 3종** — `Logo.tsx`(사이드 메뉴 FIRE EMBLEM 워드마크, currentColor·글자 간격 GAP), `IconButton.tsx`(둥근 아이콘 버튼=`.icon-btn`, 모든 모달 상단·히어로 통일), `ListRow.tsx`(배너형 행: 초상이 왼쪽서 마스크로 번지고 이름 옆에). 죽은 `.x`/`.hero-btn`/`btn-sm` 등 정리.
- **인물 명부 = 초상 갤러리** — 행→카드 격자(`char-grid`/`SortableCharCard`, rectSortingStrategy). 빈 상태 CTA("첫 인물 등록"). 헤더에 인물 수+작성(UserPlus)·반입(Download) 아이콘. 목록 모달 폭 `modal--list` 620, 셸 상단 그라데이션.
- **명부 반입** = `ListRow` 배너 적용(인물=초점 얼굴/이명, 문헌=텍스트), 드롭다운 위치 버그 수정, 모달 고정 높이+버튼 하단 고정(콘텐츠 무관), 풀네임 15자 무조건 1줄(컨테이너 쿼리 폰트).
- **편집 액션 버튼 정비** — 소각=휴지통 아이콘(좌·2클릭), 취소/기록 우측·동일 너비. **전수: 취소는 항상 확인 왼쪽**(반입·초점도).
- **.env.example**에 `GEMINI_API_KEY` 안내 추가.

### ▶ 2026-06-08 작업 요약 (보고서 탭 다듬기 — 여기서 홀딩)
인물 명부/보고서 UI·문체 집중 정리 후 **여기서 홀딩**. 주요:
- **보고서 레이아웃 개편**: 2단 → 세로 흐름 `[멘트][해시태그][능력=레이더+막대][행동 양상+잠재 심리][평판]`. 능력 막대 = `[항목 … 평가(11px) 수치]` / 게이지. 레이더 칼럼 400px·라벨 8px. **섹션 헤더 구분선**(약력 탭 신원/용모/성향/전법/비고에도 공통, 인연 토글만 제외) + '능력' 헤더. 인용구 19px·세리프 장식 따옴표.
- **새로고침**: 하단 '재작성' 텍스트 → 새로고침 아이콘 버튼(회전+비활성 연타방지). 성공 시 **상단 토스트** "(퍼스트네임)의 보고서가 갱신/발급되었습니다"(2.5초·1줄).
- **공용 자산 추가**: `Spinner.tsx`(가운데 로딩 스피너+디제틱 문구) — 목록/패널 "펼치는 중…" 전부 교체, 빈 목록은 `.list-empty` 가운데.
- **문체 규칙(프롬프트)**: 능력 한 줄 평=단일 명사구·공백포함 25자 미만(18~24자 밀도)·체언 종결·**쉼표/마침표/서술형 어미 금지**. 행동 양상=약력 〈성향〉 복붙 금지·종합·메커니즘. 분석문 **주어(그는·이름) 금지**. 평판 출처=`형용사+역할` 형식·실패조건·네거티브('수도 경비병' 금지). 해시태그=팬덤 감성·이명/본문 단어 재인용 금지. 라벨: 성격분석→**행동 양상**, 무의식분석→**잠재 심리**.
- **기계 보정(`normalizeReport`)**: 한 줄 평 끝 마침표·쉼표(첫 구만 취함) 제거, 평판 출처/코멘트 괄호류(`[]【】「」` 등) 제거. 평판 좌측 border 제거.
- **인물 반입 범위 축소**: 유저가 편집에서 작성한 것+초상만(약력·인연·이미지). **보고서·등장상태·정렬 제외**(`ImportDialog` `omit` prop). 문헌 반입은 그대로.
- **확정 규칙(CLAUDE.md)**: §1 **이탤릭 절대 금지**(`em,i{font-style:normal}`), §3 **Gemini 호출에 세계관/고유명사 사전 미주입**, §3 **프롬프트 예시(few-shot) 지양**(v1.0 앵커링 고질병). 보고서 맥락 회차요약 상한 `SUMMARY_CAP=12`·최근 본문 `RECENT_RAW=8`.
- **약력 (괄호) 부연**: 신원~비고에서 `(…)`를 작고 연하게(인연 제외, `dimParens`+Markdown).
- **public 정리**: 외부(루트)/내부(`assets/illust`·`icon`) 분리(앞 항목).

### ▶ 2026-06-08 (후반) 작업 요약 — 대륙 문헌 전면 개편 + 앵커 개선
한 묶음으로 작업 후 `main` 배포. `src/Lore.tsx`·`styles.css`·`useLore.ts`·`lib/db.mjs`·`lib/anchor.mjs`·`api/story.mjs`·`server/index.mjs`.
- **대륙 문헌 개편**(상세 §4): ①모달 헤더/버튼을 인물 명부식 통일, ②**5영역 드릴다운 배너**(일러스트 `public/assets/illust/lore-*.webp`·표지), ③**뷰/편찬 분리**(행위어 '편찬'=`UI.compile`), ④**다중 항목(섹션)** 구조(드래그 정렬·`sections` jsonb·`body`는 평문 거울), ⑤**데스크탑 2단 코덱스**(좌 목차 사이드바+우 본문, 큰 표제·여백·이전/다음 권), ⑥**모바일 풀스크린**(한 화면씩 전환).
- **편찬 다듬기**: 라벨 영역(드롭다운)·갈래(이름)·내용, 25:75(모바일 세로), **플레이스홀더 전부 제거**, 활성=뷰 북마크(체크박스 제거), '잠듦' 중복 태그 제거. **추가 버튼([+])·인연 추가**를 보고서 새로고침식 가운데 아이콘(`.add-foot`)으로. 작성 버튼 기본 하이라이트(active) 제거(인물 명부도).
- **문헌 앵커 영역화**(§6): `세력 제1권`·`가문 1권`·`세력가문 제1권`·`세력·가문 1권` 모두 인식(영역 토큰 느슨·제 생략). 번호=영역 내 순번(`volNo`·화면과 일치). 제목 겹치면 전부 주입(`find`→`filter`). bare 번호 폐기.
- ⚠️ **배포 전제(스키마)**: `alter table lore add column if not exists sections jsonb;` (없어도 동작·body 폴백, 항목 구조 영구저장은 이 SQL 후부터). 일러스트는 GitHub 웹 업로드로 `public/assets/illust/`에 둠.
- **남은 것(legacy 견문록 대비)**: 검색 · AI 문장 정제(Gemini) · 항목(권) 드래그 정렬.

### ▶ 2026-06-08 (디바이스 테스트 폴리시 + 서버 수정)
실기 테스트하며 다듬은 묶음. 주요:
- **대륙 문헌 코덱스 헤더 재구성**: 버튼 알약 스타일 유지, **모바일 닫기(X) 제거**(←로 뒤로·`lore-close-d`=데스크탑만), **작성=하단 원형 FAB**(`.lore-fab`, 목록 있을 때). 목차/본문 헤더 여백 통일(`12px 14px`). 빈 상태 문구 '기록된 문헌이 없습니다.'·저장 버튼 스피너화·북마크 hover 박스 제거·영역 설명문 손질.
- **문헌 목차 드래그 정렬 완료**(`SortableVol`/dnd-kit): 영역 안 순서 변경 → 즉시 저장, 전체 `sort_order` 인덱스 재매김(다른 영역 상대순서 보존). `Lore`에 `sort_order` 추가.
- **천각의 박동(이야기) 행 개편**: 개칭/필사/소각 텍스트 → 아이콘 버튼(`.row-actions`·소각 danger), 행 높이↑. (`.rowbtn` 정리.) → **이후(2026-06-09) ⋯ 더보기 메뉴로 접음**: 행마다 `MoreHorizontal` 한 개만, 누르면 `.row-menu`(개칭·필사·소각) 펼침. 바깥 클릭 시 닫힘(document 리스너). 버튼 3개×행 → 1개×행으로 시각 부담·증식 완화.
- **인물 명부**: ①카드 DnD 시 iOS 이미지 콜아웃·네이티브 드래그 차단(전역 `img` `-webkit-touch-callout/-webkit-user-drag none`+`draggable=false`) ②이름 옆 **사망/불명 라벨 제거**·약력 '현재 모습 안 드러냄' 멘트 제거·**비활성(잠재움) 흐림 제거**(사망 흑백과 혼동 방지, 활성은 북마크로) ③작성 버튼 기본 하이라이트(active) 제거.
- **목록 모달 내부 스크롤**(데스크탑): `modal--list` `max-height:86vh`+`display:flex`로 **그리드만 안쪽 스크롤**(스크롤이 바깥 모달-bg에 붙던 것 해소).
- **초점 지정(FaceCrop)**: 취소·지정 같은 너비·가운데(`.crop-actions`).
- **추가 버튼([+])·인연 추가**: 보고서 새로고침식 가운데 아이콘(`.add-foot`).
- ⚠️ **서버: 새 인물·문헌을 항상 맨 뒤로** — insert 시 `sort_order=최대+1`(`다음정렬값`). 원인: 새 항목 기본값 0이 드래그로 매겨진 0·1·2…의 앞 0과 겹쳐 중간에 끼던 것. **기존에 끼어 있던 항목은 한 번 드래그로 정리 필요.**

### ▶ 2026-06-09 — 본문 도중 소실(끊김) 문제 진단·해결 (실기 테스트)
**증상**: 프로덕션(모바일)에서 본문이 빠르게 출력되다 **문장 중간에 뚝 끊기고**, 잠시 뒤 화면에서 **통째로 사라짐**(에러 메시지도 없음). 앱 껐다 켜면 그 답변이 아예 없음.
**오답들(기록용)**: 토큰 초과 아님(그건 출력 전에 막힘). 네트워크 끊김 아님(같은 폰으로 대화 멀쩡). thinking 길이 추측도 틀림.
**확정 원인 = Vercel Hobby 60초 함수 제한**: 한 화 생성이 60초를 넘기면 **함수가 중간에 강제 종료** → catch도 못 돎(그래서 무에러) → DB 저장 도달 전에 죽음. 그런데 v2.0은 "서버가 **끝에 1회** Supabase 저장 → 클라가 **DB에서 다시 읽어 화면을 덮음**" 구조라, 저장이 0이면 재로딩이 화면 글을 지워 **사라짐**. (legacy는 ①Gemini Flash라 빨라서 60초 안 넘었고 ②IndexedDB 로컬 보유라 끊겨도 안 사라졌음 — 둘 다 v2.0에서 바뀐 게 원인.)
**왜 max_tokens·thinking으로 못 풀었나(확인된 제약)**: Opus 4.8은 `thinking`이 **adaptive만**(예전 `budget_tokens`는 400). effort는 이미 최저 `low`. `max_tokens`는 **자르기만** 함(완결 길이 조절 수단 아님). → 길이는 **프롬프트로**만 제어 가능(CLAUDE.md §4와 일치).
**해결(배포됨)**: 집필 지침에 **한 화 분량 기준(본문 6~10문단 안팎, 넘으면 다음 화로)** 추가 → 한 화가 60초 안에 완결. `worldview/풍화설월_집필_지침.md`(bake→`lib/worldview.mjs`). **실기 확인: 같은 프롬프트로 끊김 해소.**
**진단 중 함께 들어간 안전·정합 수정(전부 배포됨)**:
- **화수 확정 주입(§5)**: 모델이 머리글 `## 제N화`의 N을 추측해 "제42화"처럼 틀리던 것 → 서버가 N(=assistant 턴 수+1) 계산해 주입. `api/story.mjs`·`api/regen.mjs`·`server/index.mjs`.
- **스트림 끊김 대비(App.tsx)**: 받은 부분 본문을 함부로 안 덮음 — DB가 같거나 더 길 때만 채택, 끊기면 **2초×6 폴링**으로 서버 완성본 회복(있으면). id 없는(끊긴) 답변엔 **소각 버튼** 노출. `[서고 오류]` 사유 화면 유지.
- **버퍼링 방지 헤더**: `Cache-Control: no-transform`·`X-Accel-Buffering: no`(story·regen).
**취소된 것**: **로컬 우선 + 수동 저장(Supabase 수동 동기화)** 재설계 — 분량 조절로 잘림이 해결돼 **보류/취소**. (잘릴 때 사라지는 구조적 약점은 위 부분보존 폴링으로 완화. 더 필요해지면 그때 재검토 — updated_at 기반 충돌 가드 포함 설계안은 대화 기록 참고.)
**튜닝 여지**: 분량 너무 짧으면 8~12문단, 여전히 길면 5~7문단으로 지침 숫자만 조정.

### ▶ 2026-06-09 (후반) — 기록 지침 메뉴(전역 유저 커스텀 프롬프트) 신규
연대 문헌 아래 '기록 지침' 메뉴 추가. 유저가 앱에서 직접 적는 전역 집필 지침이 메인 본문(story·regen) system에 박제 SYSTEM 바로 뒤로 주입된다. 저장=Supabase `settings` 표 단일 행(key='guidance'). 자세한 건 위 §4 '기록 지침' 항목. ⚠️ **배포 전제**: `settings` 표 SQL 1회 실행(없으면 폴백). lint+build 통과 확인.

### ▶ 2026-06-09 (요약 잘림 원인·해결 + 연대 문헌 '다시 기록')
- **요약 중간에 잘리던 원인 = gemini-2.5-flash의 thinking 토큰**: 답 전에 쓰는 '사고' 토큰이 `maxOutputTokens`를 같이 깎아, `summarizeEpisode`의 700 예산이 사고에 거의 소진→요약이 문장 중간에 잘림. (보고서는 maxOut 10000으로 이미 회피했었음.) **해결**: `lib/summarize.mjs`의 `callGemini` `generationConfig`에 `thinkingConfig:{thinkingBudget:0}` — 예산 통째로 답에 → 잘림 해소. **사고는 호출별로 차등**(전역 0은 과함): `callGemini`에 `thinkingBudget` 옵션(기본 0). 화별 요약=0·출력1024(한 장면 추출), **앵커 추림=512·출력3072**, **큰 줄기 병합=1024·출력3072**(여러 화 가로질러 판단 필요 → 사고 켜되 maxOut 함께 키워 잘림 방지). ※ 사고 켜는 호출은 maxOut이 사고+답을 함께 담아야 함(보고서 maxOut1만의 이유와 동일).
- **Gemini 재시도+차단기(직전 작업)**: 503·429 등 일시적 에러 최대 2회 재시도(백오프 1→2초), 다운 감지 시 30초 쿨다운(차단기)로 무더기 호출 차단.
- **연대 문헌 '다시 기록'(신규)**: 이미 저장된(잘린) 요약은 자동 재생성 안 되므로(요약 있으면 skip), 상세 뷰에 **다시 기록** 버튼 추가 → 그 화만 재요약. `POST /api/chronicle {turn_id}`(api+server) → `getTurnContent`(db 신규)→`summarizeEpisode`→`setTurnSummary`, 응답 summary로 화면 갱신. `Chronicle.tsx` 푸터=[다시 기록][그 화로 가기].

### ▶ 2026-06-09 — 대륙 문헌 본문 존 가독성·'폼' 개편
첫 배너는 화려한데 본문이 밋밋한 문제. 원인=간격이 아니라 형식·위계·질감. 적용(전부 `.lore-doc`에만 국한): ①소주제=금빛 18px 표제(큰 표제와 본문 사이 위계 채움), ②불릿=금빛 마름모(개조식 덤프 완화·문헌 존에만). 인물 약력 등 다른 곳 불릿은 안 건드림(`.md-list` 전역 유지). ※ 세리프 본문(Noto Serif KR)도 시도했으나 **빌더 취향상 롤백**(산세리프 유지).
- **코덱스 '화려' 기준선 시도(느낌 테스트)**: 표제부에 **엠블럼**(영역 일러스트 `emblemOf` 재활용=영역 공용), 표제 아래 **금빛 마름모 장식 구분선**, 소주제에 **백합(⚜) 문양**(`.lore-fleur`, `font-variant-emoji:text`), 섹션 사이 **구분선**. 전부 `.lore-doc`에만. 엠블럼 고유 그림은 불가(에셋) → 영역 공용. ⚠️ 느낌 보고 유지/롤백 판단(이 커밋 단위로 되돌리기 가능). ⚠️ ⚜는 글리프라 기기별 렌더 차 있을 수 있음(맘에 들면 SVG로 경화).

### ▶ 2026-06-09 — 디자인 결 통일(견문록 코덱스 → 인물 명부) + 배포
견문록에 들인 '코덱스' 결을 인물 명부와 맞춤. 묶어서 `main` 배포.
- **금빛 마름모 불릿 전역화**: `.md-list li::before`를 회색 사각 → 금빛 마름모로(견문록 전용 override 제거). **견문록·인물 약력·보고서** 모두 한 곳에서 통일. 본문 서사(StoryText, 별도 렌더)는 영향 없음.
- **나침반 SVG 문양(`Ornament`) 확대**: 견문록 소주제(⚜ 글리프→SVG)에 이어, 인물 **글 섹션 제목(성향·전법·비고·인연)** 앞에도. `.view-label` flex+gap, `.view-ornament`. 신원/용모 데이터 격자(`.info-card-title`)는 깔끔히 비움(밀집 방지·내부 일관성). 파일: `src/Ornament.tsx`(단일 path, currentColor 금빛), `Characters.tsx` ViewSection·인연 헤더.
- **인물 명부에 적용 안 함(의도)**: 엠블럼(초상이 대체)·작성 FAB(헤더 ＋명부추가 유지)·장식 가로구분선(섹션 밑줄로 충분). 소주제 금빛 제목+밑줄·add-foot 미니 인장은 **이미 있던 것**.
- (직전 묶음) 견문록 코덱스: 엠블럼(영역 공용)·금빛 장식 구분선·섹션 구분선·작성 FAB '인장'(양피지+금빛테+깃펜, 46px)·add-foot 미니 인장·`scrollbar-gutter:stable both-edges`(권 전환 덜컹임 해소). 세리프 본문은 시도 후 롤백.

### ▶ 2026-06-09 (디자인 통일 + 배포 파이프라인 정리)
실기 보며 UI 결을 한 흐름으로 맞춘 묶음. 모두 배포됨.
- **배포 정리**: Vercel Git 연동 해제 → Action 단독(위 §2 ✅). 한도/큐잼 사고 근본 해결.
- **'인장' 버튼 언어**: 작성 FAB(양피지+금빛테+깃펜 46px) · 추가(＋) 버튼(`.add-foot`) · 동작 버튼(`.editor-actions .primary`·`.guidance-save` = 기록/저장/지정/반입) 전부 **어두운 양피지+금빛 테두리·글자 알약 인장**으로. 취소=차분 알약. 단, **연대 문헌 푸터 [재작성][이동]은 밋밋한 알약**(인장 아님, 빌더 지정).
- **나침반 SVG 문양**: ⚜ 글리프→`public/assets/ornament.svg`를 CSS `mask`로 찍어 currentColor(금빛). **모든 섹션 제목**(신원·용모·능력·평판·성향·전법·비고·인연 + 견문록 소주제) `::before`로 일괄. `Ornament.tsx`는 미사용(잔존).
- **구분선=금빛 장식선**: 섹션 제목 밑줄을 평평한 회색→금빛 그라데이션(`border-image`). **모달 헤더 표준화**: `.modal-head` 기본에 구분선 → 연대 문헌·기록 지침·천각의 박동·반입까지 "제목+X+구분선" 일치(목록형 중복 규칙 통합).
- **금빛 마름모 불릿**: `.md-list li::before` 전역(견문록·인물 약력·보고서 공통, 본문 서사는 별도).
- **연대 문헌**: 목록(2줄 미리보기)→상세 뷰+**이동(그 화로 점프, `jumpToTurn`)**, **재작성**(단일 화 재요약 `POST /api/chronicle`), 모달 최소높이 420px, 푸터 우측 그룹.
- **대륙 문헌**: 영역명 제목 제거(헤더엔 뒤로가기+구분선, 목록만). 엠블럼(영역 일러스트) 시도→제거. 본문 스크롤바 거터 고정. 세리프 본문 시도→롤백.
- **인물 명부**: 인연 **편집도 기본 접힘**, 펼침 시 구분선·여백을 비고와 일치(`.bonds-head{line-height:1.8}` — 버튼 기본 line-height 차이가 원인이었음).
- ⚠️ **교훈(반복 실수)**: 새 라벨은 반드시 `src/strings.ts` `UI`에서 꺼내 쓸 것(§1) — "다시 기록" 직접 타이핑 등 어긋남 반복. 지시는 한 번에 정확히 읽고 애매하면 묻기. 배포는 빌더 "배포해" 때만.

### ▶ 2026-06-09 — Gemini 호출 공용 클라이언트로 통합(legacy 패턴 계승)
**계기**: 보고서 Gemini가 503에 계속 죽었는데, 원인은 **재시도가 요약(summarize.mjs)에만 있고 보고서(report.mjs)엔 없어서**였다. legacy엔 `services/gemini/client.ts` **공용 클라이언트**(503/429/5xx/타임아웃 재시도+지터 백오프)가 있어 모든 호출이 보호됐는데, v2.0이 호출을 기능별로 흩어 짜며 그 패턴을 안 따른 게 화근(= "한 군데 재시도 빠뜨림" 버그).
**해결**: `lib/gemini.mjs` 신설(공용 `callGemini`) — 재시도(429·500·502·503·504) + **지터 백오프**(3^n×기준+0~2초, 429는 더 길게) + **타임아웃 120s**(AbortController, 멈춤 방지 — v2.0엔 없었음) + **모듈 차단기**(다운 감지 시 30s 쿨다운). `summarize.mjs`·`report.mjs`가 전부 이걸 거친다(직통 호출 0). **앞으로 편지·해시태그 붙일 때도 import만 하면 자동 보호.** ⚠️ 새 Gemini 기능은 반드시 `lib/gemini.mjs` 경유 — 직접 fetch 금지.

### ▶ 2026-06-10~12 — 임무·소지품 탭 완성 + 에셋 파이프라인 + 표준 3종 (아이템 콘텐츠 일단락)
여러 배포(run #26~) 묶음. 상세 스펙은 §4 인물 명부 항목 참조. 주요:
- **대륙 문헌 5영역 = 세력축 개편**(제국·왕국·동맹·교단·만상) + 빌더 제작 휘장 일러스트. 기존 문헌은 SQL로 왕국 일괄 이동. 앵커 영역명 동기화.
- **운명의 장 '환원'**(UI.revert) — 장은 남기고 본문·연대 문헌만 백지로. 더보기 메뉴+확인 다이얼로그. `DELETE /api/turns?story_id=`.
- **임무 탭**(legacy 퀘스트 이식) + **소지품 탭**(legacy 인벤토리 이식 — 누적 탐색·두 번 누르기 소각·드래그 정렬).
- **소지품 그림 시스템 완성** — 빌더 제작 일러스트 **487장**(폴백1+486종, 11분류). emoji 방식 계승: AI가 분류 묶음 후보에서 라벨 선택→박제. 별칭 흡수+분실물 대장 로그. 폴백 누수 버그(이웃 소각 시 전염) 수정 — 아이콘은 icon prop만의 함수로. 에셋 명세 = `docs/에셋제작_소지품.md`(생성 프롬프트 포함).
- **표준 3종 신설(CLAUDE.md §1)**: ①**메시지 전달 표준** — 전역 토스트(`src/toast.tsx` showToast/ToastHost) 단일 통로, 인라인 에러·브라우저 alert·자체 토스트 전폐. 동사 일관(보고서=발급/갱신, 임무=전달/갱신, 소지품=확인, 검증=명령형). ②**ESC = 맨 위 한 겹만**(`src/useEscClose.ts` 스택, Modal 내장). ③**헤더 문법** — 왼쪽=동작·뒤로, 가운데=제목, 오른쪽=닫기뿐.
- 로딩 스피너 문구 전폐(스피너만). 임무·소지품 새로고침 버튼 = 돋보기+로딩 시 표준 스피너.
- ⚠️ 소지품 아이콘 명단(`lib/itemIcons.mjs`)은 **분류별 정리·문서와 동시 재생성**(카운트 정합). 새 그림 등재 절차는 에셋 명세 참조.

### ▶ 2026-06-12 — 모바일 드래그 정렬 해결(스크롤 선점 차단) + 소지품 중복 완화
실기 테스트 묶음. 전부 배포됨(run #36·#37 success).
- **모바일 드래그 정렬 = 해결(실기 확인)**: 증상 = 꾹 눌러 물건이 집혀도 끌면 스크롤이 같이 움직여 제자리(스크롤 맨 위/아래에선 정상 — 결정적 단서). **원인 = iOS 웹킷의 스크롤 '선점'**: 누름 초기 몇 px 잔움직임(tolerance 허용 구간)에 브라우저가 스크롤을 가져가면, 그 손짓이 끝날 때까지 dnd-kit이 못 막음(touchmove 취소 불가). **해결 2단**: ①`src/useSortSensors.ts` 신설 — 공용 센서 훅(PointerSensor 8px / TouchSensor 길게 눌러 집기 220ms·손떨림 10px) + **스크롤 선점 차단 가드**(문서당 리스너 한 벌·참조 세기): `data-sort-guard` 카드에서 시작한 터치는 손떨림 반경 안 touchmove를 선제 preventDefault(선점 차단), 반경 밖(=플릭)은 즉시 비켜섬. ②드래그 대상 4곳(인물 카드·소지품 카드·인연 행·문헌 목차)에 CSS 터치 가드 일관 적용(`touch-action: manipulation`+iOS 콜아웃/선택 차단 — char-card·bond-row·lore-vol엔 아예 없던 것) + `sortGuardProps` 표식. **새 정렬 목록을 만들 땐 둘 다 필수**: `useSortSensors()` + 카드에 `{...sortGuardProps}` + CSS 터치 가드. 트레이드오프: 카드 위에서 아주 느리게 시작하는 스크롤은 '집기'로 읽힐 수 있음(플릭은 무관) — 거슬리면 반경 숫자 조율.
- **소지품 탐색 중복 완화 2겹**(`lib/items.mjs`): 같은 물건("낡은 가죽 손목 보호대")이 그대로 두 번 들어오고, '흙 묻은 돌→조약돌→자갈'류 근접 변주가 떼로 몰리던 문제. ①**코드 차단(결정적)** — runItems가 기존+이번 묶음과 이름 겹침(공백·대소문자 무시)을 떨굼, 전멸 시 "새로운 물건을 찾지 못했습니다" 안내(재호출 없음 — 비용 0, 다음 탐색이 채움). ②**프롬프트 압박** — 기존 목록에 '결이 닮은 변주 전부 금지' 명시 + 다양성 규칙(3점은 서로 다른 갈래, 의미 없는 자연 파편으로 자리 채우지 말 것). 근접 중복을 코드로 자동 판별하지 않은 이유 = 멀쩡한 물건 오인 제거 위험.
- 에셋 문서 stale 주석 정리(`docs/에셋제작_소지품.md` "222장 반입 중"→"487장 확정") + 아이콘 명단 무결 검증(key·label 중복 0, 파일 487장 정합).

### ▶ 2026-06-13 (밤) — 기록 지침 = 장별 분리 (설계 변경)
원래 '전역'(모든 이야기 공통 주입)으로 설계했으나, 실사용에서 "장을 옮겨도 지침이 따라간다"가 어색 → **장(이야기)별 분리**로 변경(빌더 확정). 저장 키 = settings 표 `guidance:{storyId}`(**스키마 변경·SQL 없음**). `db.mjs`(getGuidance/setGuidance에 storyId, deleteStory 시 키 정리, copyStory 시 지침 동반 필사) + `api/guidance.mjs`·서버 라우트(story_id) + 주입 4곳(story·regen×로컬·배포) + `Guidance.tsx`(storyId prop, App이 현재 장 전달). ⚠️ **옛 전역 행(key='guidance')은 더 안 읽음** — 기존 지침은 원하는 장에서 한 번 다시 기록 필요(내용이 기억 안 나면 Supabase settings 표 key='guidance' 행에 남아 있음).

### ▶ 2026-06-13 (후반) — 서신 2차 라운드: 모브 검증 완료 + 흐름 확률 교정 → 배포
실기 계속하며 마지막 조각(모브)까지 검증·교정. **상세 규칙은 `docs/서신_설계.md` §12(실기 라운드 보정)** — 본문 §과 충돌 시 §12 우선. 골자:
- **흐름 확률**: 답장 시도율 50%(답장 독점이 신규·모브를 굶기던 것 — legacy '40% 스킵' 부분 복권) + 모브 고정 40%(가중치 희석 탈피, 주사위 선행). 다이얼 3종 = `답장시도율`·`모브비율`·`역할반전`(lib/letters.mjs 상단 상수).
- **구금 게이트**: 판별에 confined 추가 — 옥중 인물은 발신·답장 불가(벨레스 옥중 편지 사고). 매번 최근 기록을 읽어 석방 자동 반영.
- **유서**: 수신자의 사후 칭호도 방화벽(아버지가 아들에게 '폐하' 사고) / 온도는 인물 기록이 정함(패트리샤 OOC) / **유서없는자 규약**(안젤마 — 빌더 확정, 소각해도 재발견 안 됨).
- **모브 두 얼굴**: role(직함—목록용)·signature(서명—자유, 드림 허용) 분리. '(직함)' 자리표시자 누출·본문 끝 서명 중복 수정. 직함 규율 완화(짧은 한정어 1 허용 — '시장 상인').
- **소문 입말화**: gist가 본문 관청 용어를 그대로 나르던 것 → 입말로 닳게.
- **이전 서신의 쓰임**: "결은 잇되 대화는 잇지 않기" — 답신투·화제 재탕 금지(잉그리트 반복 편지 사고).
- 실기 합격 샘플: 실뱅(거리 게이트·문단·서명) / 디미트리→펠릭스 draft(자발 draft의 정수) / 여인숙 주인(모브 두 얼굴·입말화). **서신 = 인물 명부 다섯 탭 완결.**

### ▶ 2026-06-13 — 서신 실기 다듬기 라운드 (로컬 검증 완료 — 품질 합격, 미배포)
빌더가 로컬(dev)에서 실기 테스트하며 품질 어긋남을 잡고 한 겹씩 조인 라운드. **거리 게이트·문단·서명·draft 전부 실기 검증됨**(실뱅 편지 = 합격 샘플). 주요:
- **유서 시간 방화벽**: 죽은 그렌이 '더스커의 비극'(사후 명명) 언급 → 발신자·**수신자**·관계 기록 전부 "사후 시점 자료" 명시 + 죽음 회고·사후 칭호(폐하 등) 금지. 재발급으로 검증 ✓.
- **호칭 예법 4규칙**: ①관계>신분(아버지는 아들 왕에게 '아들아') ②시점 기준(안 오른 자리 경칭 금지) ③겹침 금지('네 아버지 람베르 폐하' ✕) ④자기 경칭 금지('폐하의 명'을 왕 본인이 ✕) + 한 통 안 경칭 일관(전하/폐하 오락가락 ✕).
- **본문 개행 버그(치명)**: 정리()의 `\s{2,}`가 빈 줄을 뭉개 문단이 전부 붙음 → 본문 전용 `본문정리`(개행 보존) 분리. + 프롬프트 문단 규칙(2~4문장마다 빈 줄, \\n\\n 명시).
- **draft 게이트(치명)**: 상대(子·모브)가 draft 선택 → 고아 행(토스트만 뜨고 안 보임) → **자발적 draft는 함 주인 발신일 때만**(프롬프트에서 선택지 제거+코드 차단, 어기면 침묵 처리). 고아 청소 SQL: `delete from letters where status='draft' and sender_id is null;`
- **영문 서명 폴백 2단**: 한글로만 서명 시 ①명부 english_name ②**NAME_DICT**(bake가 한→영 사전을 `lib/worldview.mjs`에도 구움 — 미등록 인연 커버)로 치환. 잉그리트 사례.
- **표기교정 추가**: 람베르트→람베르(실기 발견 — 새는 표기는 이 배열에 보탬). 모브 별칭은 **외래어만** 접기(하녀≠시녀·집사≠시종 — 직분 뭉개기 금지, 빌더 정정).
- **Gemini 무료 한도**: 429 RPD(일 20회) 소진 시 재시도 생략·즉시 안내. 서신 1회=2호출(판별+생성)이라 빨리 닳음 — **빌더 결제 연결 권고**. 에러 문구 디제틱화(차단기→"먼 곳의 응답이 뜸합니다…", 토스트에 기술어 필터).
- **UI**: 깃펜 버튼=패널 푸터 FAB(`.letter-fab`=lore-fab 공유) / 목록 퍼스트네임(bondNames prop) / 제목 글자수 제한→형태 규칙+2줄 클램프 / 행 여백·유형 라벨 제거(내부 분류 비노출).

### ▶ 2026-06-12 (밤) — 서신 시스템 1차 구현 (도면대로 — 실기 미검증)
`docs/서신_설계.md` 도면대로 전체 골격을 지음. **lint·build·로컬 라우트 스모크 통과, 단 Gemini·DB 실호출은 미검증**(샌드박스에 키 없음 — 빌더 실기 필요).
- **서버**: `lib/letters.mjs`(교환소 엔진 — 후보모으기/판별(Flash#1: 거리·등장·사건 공개성)/지식블록 3단/생성(Flash#2)/행만들기 후처리) + `lib/db.mjs` letters CRUD + `api/letters.mjs`·`server/index.mjs` 라우트(GET 목록/POST 생성/PUT 편집·개봉/DELETE 소각). 흐름 = 유서(전세계 1통) → 답장(침묵 가능) → 신규(거리 가중 추첨+역할 반전 40%+모브 한 자리). 사망·실종 인연이 뽑히면 母의 '부치지 못할 편지'(draft 강제).
- **UI**: `src/LettersTab.tsx`(수신함/발신함/보관함 + 봉인 배지·미독 점 + 상세 뷰(편집·소각) + 서명 `한글 수식어+영문 이름(.script Pinyon)` 렌더 + 깃펜 새로고침). `Characters.tsx` 서신 탭 장착(EmptyTab 제거 — 탭 5종 완성). `strings.ts`에 수신함/발신함/보관함 등재. 토스트: 당도/유품 발견/보관함.
- **CLAUDE.md**: §3 승인 예외(서신 한정 — NAMES+호칭 체계+금지) + §1 동사 '서신=당도' 등재.
- ⚠️ **배포·실기 전제**: Supabase SQL 1회 — `letters` 표 생성(`docs/서신_설계.md` §1의 스키마 그대로). 없으면 "서신함이 아직 마련되지 않았습니다" 폴백.
- ⚠️ 알려진 한계(1차): 이야기 필사(copyStory)에 서신 미포함 · 판별 실패 시 안전 폴백(전원 '나라' 거리·미등장 — 관계만으로 작성) · 모브 직함 별칭 표는 최소(분실물 대장 로그 보며 보강).

### ▶ 2026-06-12 (후반) — 서신 시스템 설계 확정 (미구현 — 도면만)
빌더와 긴 설계 논의 끝에 서신 시스템 전체 설계를 확정. **상세 도면 = `docs/서신_설계.md`** (착수 시 그 문서가 기준 — 이 항목은 색인). 골자: 새 `letters` 표(영구 누적·답장 깊이 1) / 발신자 자격(활성 게이트·母子 쌍·생사 게이트) / **지식 판별 2축**(사건 공개·사적 × 발신자 등장 여부 — 판별과 생성 분리, 줄거리 공급은 코드가 on/off. legacy 발작의 근본 처방) / 위치→빈도(거점 우선·유랑 최저) / 모브 열린 명단+표기 안정화(8자 해제) / 수동 1통·침묵의 권리·역할 반전 40%·유서 전세계 1통 / Draft 보관함(자발·사망자 앞·답장 draft, 빈도는 성격에 맡김) / 용어 B안(NAMES+호칭 체계+금지 — 승인 예외 추가 예정) / UI 수신함·발신함·보관함, 서명=`한글 수식어+영문 이름(Pinyon)`. ⚠️ 착수 전: letters 표 SQL 1회(설계서 §1), CLAUDE.md §3 승인 예외 추가.

### ▶ 2026-06-13 (서신 이후) — 본문 품질 대개편 · Vercel Pro · 소지품/보고서/임무 화법 · UI 다듬기 묶음
서신 완결 뒤 같은 날 이어간 큰 묶음(이 항목은 렉으로 누락됐던 것을 사후 정리 — 전부 `claude/laughing-cori-w4blxq`에 커밋됨). 골자:

- **본문 품질 대개편(집필 지침 재작성)** — `worldview/풍화설월_집필_지침.md`(+ `규약` 정합절). Opus가 금지 규칙에 눌려 위축되던 '살얼음 모드'를 풀려고 **작법(장면·보여주기·밀도)을 앞세우고 금지 규칙은 안전장치로 강등**(0664f91). 이어 ①**지명·지리 검문**(앙바르 사고 — 실재 지명 함부로 안 만듦), ②**사실 검문**(무기·신체·생사) + **문장 안 셀프 정정 금지**, ③**정합 절 신설**(장소·거리·회상·하루의 길이) = 사건이 벌어진 곳과 인물·물건의 현재 위치를 따지고, 먼 곳 일은 보고만 먼저 닿으며(이동엔 시간), 앞 회차에 적힌 과거 디테일은 확정(회상은 새 연출이 아니라 기록)으로 못박음. ⚠️ **예시 금지(§3) 자해 교훈**: 보강하며 넣은 예시들이 본문에 그대로 새어(아래 임무 '결.' 사고와 같은 결) → **추가분 예시 전부 제거하고 추상 원칙으로 압축**(38c4878). ④**분량 상한 해제**: 한때 '8~12문단' 숫자를 넣었다가, 길이는 숫자가 아니라 **'한 화 = 한 장면' 원칙**이 다스리게 도로 풂(387ec24). 자꾸 길어지면 분량이 아니라 사건을 너무 담은 신호 → 장면을 끊어 다음 화로.
- **메인 본문 effort low → medium**(e9be716, 빌더 승인 '사실 정밀도 실험'). `api/story.mjs`·`api/regen.mjs`·`server/index.mjs` 셋 다 `output_config:{ effort:'medium' }`. ⚠️ **이게 가능해진 전제 = Vercel Pro 전환**(아래) — effort를 올리면 한 화가 길어져 옛 Hobby 60초 천장에 다시 걸렸을 것. 끊김 재발 시 주석대로 low 회귀 가능.
- **Vercel Pro 전환 — 60초 천장 해제**(eeee366): `api/story.mjs`·`api/regen.mjs`에 `export const config = { maxDuration: 300 }`(60→300초). 2026-06-09에 진단했던 'Hobby 60초 함수 강제종료 → 긴 본문 소실' 문제의 **근본 해소**(그땐 분량 축소로 우회했으나, 이번 품질 개편으로 분량 상한을 풀면서 시간 여유가 필요해짐). 비용 가드 = **Anthropic 선불 캡 + Vercel spend limit $20**. (`api/chronicle.mjs`는 60 유지 — 요약은 짧음.)
- **소지품 화법 철학 재정립**(`lib/items.mjs`, 여러 커밋 af67dad→4bf7bd9): ①**보여주되 말하지 않기** — 주인의 성격·내력을 단정하지 말고 물건 상태로만, 결론은 독자 몫. ②**주어는 늘 물건** — 주인의 행위·심정을 끌어들이지 않음. ③**간결** — 관찰 하나만(나열·원인결과·이름 반복 금지). ④**지역 풍미는 살리되 내력은 금지** — 지역·재질=물건의 정체(허용), 개인 사연만 금지(충돌 해소). ⑤**현재 국면 우선(맥락 > 약력)** — 임무와 같은 결로 지금 처지가 물건을 좌우(갇힘·빼앗김이면 가진 게 없다 = 서신 구금 게이트의 소지품판), 약력만 보고 도돌이표 찍지 않게.
- **보고서 — 본문 우선·복붙 금지**(`lib/report.mjs`): ①**인용구 복붙 금지**(d04f493) — 본문·줄거리·작가 지시에서 따오지 말고 새로 빚게. ②**행동 양상·잠재 심리 = 최근 본문 우선**(5ae3ef0) — 약력은 바탕일 뿐, 연재 진도를 따라 변하게.
- **임무 말투 굳음 수정**(`lib/quests.mjs`): ①설명문에 어미 예시('~해두자 결')를 넣었더니 **'결.'이 그대로 베껴져 본문에 누출**(dccc07f) → 예시 제거하고 원칙으로. ②예시를 떼자 이번엔 **혼잣말 톤까지 같이 사라져** 당위문처럼 딱딱해짐 → **당위형 금지 + 혼잣말 어조 복원**(예시 없이, ab9a4f0). **교훈 = §3 few-shot 자해의 교과서적 사례**(예시는 베껴지고, 떼면 톤이 빠진다 → 규칙·금지·어조 묘사로만 유도).
- **UI 다듬기 묶음**:
  - **자동 스크롤 = 바닥 근처일 때만**(1b3293a): 위쪽 옛 화를 수정하는 중에 새 출력이 화면을 빼앗던 것 → 바닥 근처에서만 따라가고, '전개'(전송)는 명시적으로 스크롤. `src/App.tsx`.
  - **iOS 편집 커서 키보드 밀착 완화**(feb1f56): 편집칸 높이 상한 해제(내부 스크롤 제거) + `scroll-padding`으로 숨통.
  - **버튼 로딩 스피너 휨 버그**(4780c66): 스피너가 아래로 휘던 것 = `transform` 정렬 충돌 → `inset`+`margin` 정렬로 해소(`Button`/`.btn`).
  - **소지품 카드 제목 14px → 13px**(56cfd40).
  - **읽기 모드 화 간격 2배 확대**(4e1b309→e4a1b4b, 22→55→110px): 소설처럼 화 사이 숨을 키움.
  - **아이콘 회전 철거 + CLAUDE.md 규칙 등재**(74cd064): 보고서 재작성 로딩을 아이콘 spin이 아니라 **표준 스피너**로 교체, `.spinning` 제거. → CLAUDE.md §1 '아이콘을 회전시키지 않는다(확정)'.
  - **서신 편집 시 봉인 되살아나던 버그**(e232bcf): 열람 때 `sel`도 함께 개봉.

### ▶ 2026-06-13 (밤늦게) — 참조 자료 '받아 적기' 정조준 + 맨아래로 FAB + 브랜치 정리
실기하며 잡은 묶음. 작업 = **`claude/wizardly-dirac-WjCg3`(옛 작업 브랜치로 복귀)**. 주요:

- **본문이 인물 메모를 그대로 베끼는 문제**(`lib/charContext.mjs` + `worldview/풍화설월_집필_지침.md`): 인상 "여신 같은 미모" 같은 평가·비유가 본문에 직출. 원인 = 기존 가드가 *얼마나 자주 쓸지*(나열 금지)만 막고 *표현을 베끼지 말라*는 말이 없었음. → 주입 블록 + 집필 지침(참조 정보 절) 양쪽에 "설정값은 메모지 본문 아님, 평가·비유를 서술로 받아 적지 말고 행동·반응으로 풀어 보여줄 것" 명시(bbadf6c). 집필 지침은 bake로 박제 반영.
- **견문록(설정 문헌) 받아 적기**(`lib/loreContext.mjs`): 왕궁 구조도를 넣으니 모델이 구역을 나열("후방 지원 구역의 세탁소…")하고 장소를 정의·해설("내궁. 왕실의 가장 안쪽…"). 실기로 견문록 문장 "왕비와 왕족의 거처는 … 별도 동으로 구성된다"가 본문에 **paraphrase로 새는 것** 확인(스샷). → 가드 강화: ①참조 자료지 본문 아님 ②**그대로든 paraphrase든** 옮기지 말 것 ③"A는 B로 구성된다/들어설 자리" 정의문 말투 자체가 베낀 티 ④지리·구조는 장면이 놓인 한 자리만 인물의 눈·몸으로 감각, 다른 구역 훑기·해설 금지 ⑤설정 사실은 인물 행동·시선에 간접적으로만(c10a1f5→310e433).
- **교훈**: 인물 메모·견문록 둘 다 "**참조 자료를 본문으로 받아 적기**"라는 한 뿌리. 집필 지침의 큰 원칙만으론 부족 → **주입 블록마다 그 자료 특성에 맞는 구체적 가드**가 잘 듣는다. ⚠️ 둘 다 서버 런타임 코드(bake 무관) — 로컬 반영엔 dev 서버 재시작(express는 자동재시작 아님) + 빌더 로컬은 `git pull` 선행.
- **다음 후보(가드로도 새면) — 프롬프트 설득 아닌 입력 축소**: ①구조도·지도류 견문록을 본문 상시 주입에서 빼고 **앵커 지목 시만** 꺼내기 ②"한 화에 새로 호명하는 고유 장소명 1~2개 제한" 수치 가드. 입력 자체를 줄이는 쪽이라 더 확실.
- **본문 우하단 '맨 아래로' FAB**(`src/App.tsx`·`styles.css`): 앱 진입이 1화 위쪽이라, 바닥에서 320px 이상 멀어지면 뜨는 금빛 FAB(`.to-bottom-fab`, `.lore-fab` 비주얼 공유)로 가장 최근 기록에 한 번에. 입력바 위로 띄움(`.page.write`/`.read` bottom 분기), 아이콘 `ChevronsDown`, 진입·내용 변화 시 즉시 표시 판정(a4ca269). 디제틱 라벨 "가장 최근 기록으로". ⚠️ 입력바 여러 줄로 길어지면 드물게 겹칠 수 있음(필요 시 입력바 높이 실측으로 보강).
- **브랜치 정리**: 세션이 3개(main·wizardly-dirac·laughing-cori)로 늘어 **main + 옛 작업 브랜치(wizardly-dirac) 둘**로 합침. 모두 같은 커밋으로 ff 후 laughing-cori 폐기 — 단 **세션 묶임으로 원격 laughing-cori 삭제만 프록시가 막음**(빌더가 GitHub Branches에서 직접 삭제). 이후 작업은 wizardly-dirac에서 이어감.

### ▶ 다음 할 일 (우선순위)
0. **신규 콘텐츠("우리만의 new") 후보 = `docs/신규_콘텐츠_구상.md`** — legacy 이식 완결(2026-06-13) 후의 다음 방향 메모(포드라 달력·저잣거리 소문지·유저 직접 서신·모브 승격·음유시인의 노래). **미확정** — 빌더가 고르면 서신 때처럼 설계 논의부터.
0-1. ~~**프로덕션 반영 조건(보고서)**~~ — ✅ **해결(2026-06-08)**: 빌더가 ① **Vercel 환경변수 `GEMINI_API_KEY`** 추가, ② **Supabase SQL** `alter table characters add column if not exists analysis jsonb;` 둘 다 완료. 프로덕션에서 보고서 발급 작동.
   - ⚠️ **의존성**: `@dnd-kit/*`·`lucide-react`. **git pull 후 `npm install`**(로컬). 배포는 Action이 설치.
1. ~~서신 시스템~~ — ✅ **완료(2026-06-13)**: 구현+실기 검증+배포. 도면 = `docs/서신_설계.md`(§12 보정까지). **인물 명부 탭 5종 전부 완결.** 남은 다듬기는 운영하며(표기교정·별칭·유서없는자 명단은 발견 시 한 줄씩, 다이얼 3종은 체감으로).
   - **서신 확장 — '수신 지정'(유저 지정발신)**: 설계 확정·**미구현 도면 = `docs/서신_설계.md` §13**(2026-06-13). 인물 A의 서신 탭에서 A가 인연 중 수신자를 골라 쓰게 함 — AI는 항상 쓰되 발송/draft는 인물 판단(침묵의 권리 → draft 권리). 발신자 지정 ≠ 지식 주입(§3 판별 그대로 = 누설 안전). 상태 게이트: A사망=불가/A구금=draft/수신자 사망·실종=draft. 착수 시 §13 기준.
   - ~~임무·소지품 탭~~ — ✅ **완료(2026-06-10~12)**. 인물 명부 탭 5종 중 서신만 남음.
   - **보고서 탭(완료·성숙)** = Gemini Flash 자동 발급 분석 보고서. 파일: `lib/report.mjs`(`buildReportPrompt`·`normalizeReport`·`runReport`) + `api/report.mjs`·서버 `POST /api/report` + `db.mjs`(`getCharacter`·`setCharacterAnalysis`·인물칸 `analysis`) + `Characters.tsx`(ReportView·StatRadar·StatBar·`발급()`). 구성: **능력치 8종**(무력·마력·신앙·지성·**입지**(권세+위상 합침)·재력·매력·정신, 레이더+막대+한 줄 평) + **행동 양상·잠재 심리** + **평판 6종**(타인의 시선, 외부 인연=상호성 엔진) + 인용구 + 해시태그 5. 맥락=**3겹**(큰 줄기+회차요약[상한 `SUMMARY_CAP=12`]+최근 본문 `RECENT_RAW=8`화, 작가 지시 제외·읽기 전용). thinking 예산 2048·maxOut 10000. 발급 조건: `GEMINI_API_KEY`+`analysis` 컬럼(없으면 안내 폴백). 문체·기계보정·반입범위·규칙은 위 2026-06-08 요약 참조. ⚠️ 미반영: 임무·소지품(레거시는 같은 호출에 포함)·인연 테마색.
2. ~~**대륙 문헌(Lore) 뷰/에디트 모드 복제**~~ — ✅ **완료(2026-06-08)**. 5영역 드릴다운+뷰/편찬 분리+다중 항목+데스크탑 2단 코덱스+이전/다음 권까지. 남은 것: legacy 견문록의 **검색·AI 문장 정제(Gemini)**(우선순위 미정).
3. **실기 테스트** — 미실시. 진입 전 summary 컬럼 SQL 확인. 충전=소액 선불+자동충전 OFF. 확인: 요약 품질·앵커·메타 누출·비용.


- **컨텍스트 윈도우 + 연대 문헌 — B구조(화별 요약 단일 진실)로 재작성**: 옛 화는 **화별 요약**으로 클로드에 주입(최근 2화=원문). 같은 화별 요약이 **연대 문헌(유저 읽기)**에도 쓰임 — 단일 진실. 파일: `lib/summarize.mjs`(`summarizeEpisode(body)` = '왕실 사관' 규칙, **결과 본문만 입력(유저 프롬프트 미투입)**·심리추측/미사여구/날짜 버림, 사건·인과 + 명시적 관계·상태 변화 0~3) + `lib/chronicle.mjs`(parseEpisodeTitle[본문에서 제목 따옴, 재생성 X]·ensureEpisodeSummaries[없는 것만 lazy 생성·저장]·buildChronicle·assembleContextSummary) + `lib/memory.mjs`(prepareConversation이 옛 화 요약 보장 후 '【제목】 요약' 시간순 조립) + `db.mjs`(loadTurnsForSummary[summary 컬럼 없으면 컬럼 없이 재시도]·setTurnSummary). **스키마**: `turns.summary text`(SQL: `alter table turns add column if not exists summary text;`). **연대 문헌 메뉴**: `src/Chronicle.tsx`(GET `/api/chronicle?story_id` → 화별 제목+요약, 빠진 요약 lazy 생성) + App 메뉴 4번째. **안전 폴백**: summary 컬럼 없거나 Gemini 실패 시 그 화는 본문으로 폴백(맥락 유실 0). 옛 `stories.summary` 롤링 방식은 폐기(컬럼은 잔존·미사용). **요약 무효화**: 다시 받기(regen)는 `updateTurn(...,true)`로 그 화 요약을 비워 lazy 재생성(내용 갈아엎기 대응). 수정(문체·단어)은 요약 유지(괜한 재요약 방지). **주입 = 3겹(중요)**: ①**큰 줄기**(전체 흐름, 아주 옛 화들을 거시 줄거리로 압축) + ②**최근 화별 요약**(큰 줄기에 안 녹은 최근 화) + ③**최근 2화 원문**(messages). `memory.mjs`(WINDOW_MIN=20·WINDOW_MAX=30·KEEP_TURNS=5) + `summarize.mjs`(summarizeArc '대연대기 편찬자') + `chronicle.mjs`(ensureSummariesAt/assembleFromIndices). **큰 줄기 갱신 = 묶음(B안)**: 안 녹은 화가 30 넘으면 오래된 것부터 20 남을 때까지 한 번에 접음(≈10화마다 Flash 1회) — 자주 안 써 안정적·쌈. 큰 줄기는 화별 요약에서 굴림(원문 재독 없음). **저장**: 큰 줄기=`stories.summary`, 흡수 포인터=`stories.summary_turn_id`(옛 롤링 컬럼 재활용). **빈틈 0**: 안 녹은 화는 갱신 주기와 무관하게 ②로 주입. 그래서 주입량은 화수 무관 일정(원문2 + 요약20~30 + 큰줄기1). 연대 문헌(읽기)은 전 화 요약 그대로 보관. ⚠️ regen·copyStory는 요약 미적용. ⚠️ 연대 문헌 첫 열람 시 미생성 요약 버스트 가능.
- **앵커링/선택 주입 — 적용 완료(회차 단위)**: 유저가 본문 입력에 `"N화 참고"`/`"N화 참조"`(범위 `79~81화`, 다중 `79,80,81화` 지원)라 쓰면, 서버가 그 회차를 통째로가 아니라 **목적(문체/사건/대사/감정선/장면 대구)에 맞춰 요약·발췌·전문 중 하나로 추려(Flash) 주입**. `lib/anchor.mjs`(parseAnchors·buildAnchorContext, v2는 N번째 서술자 본문=N화) + `lib/summarize.mjs`(pickAnchorBrief, 공용 callGemini로 정리) + story.mjs·server 양쪽 주입(시스템 맨 끝=대화에 가장 가까이). UI: `App.tsx`가 본문마다 `.ep-no`("N화") 표식 — 앵커 기준점. **opt-in**이라 Flash 비용은 지목 시에만. Flash 실패/회차 없음이면 앵커 없이 진행(안전). legacy `contextOptimizer.ts` 계승하되 '무조건 전문' → '목적별 형태'로 한 수 더.
- **고유명사 사전 선택 주입 — 안 하기로 결정(2026-06-06)**: 사전(~17KB)은 **철자(정발 표기) 정답표**일 뿐 지식원이 아니다(캐릭터 이해는 Opus 학습 데이터에서 옴). 게다가 박제 SYSTEM에 들어가 **프롬프트 캐싱**을 타므로 매 턴 부담이 이미 낮다. 선택 주입으로 트리밍하면 **못 잡은 이름의 철자 흔들림** 위험만 생기고 절감은 작다(마진 < 리스크). → **사전은 통째로 유지.** 선택 주입의 실효는 동적·비캐시 payload(인물 상세=B정책, 견문록=활성토글+앵커)에서 이미 챙기고 있음. ⚠️ 재검토 시점: 사전이 훨씬 커지거나 캐시가 자주 깨질 때.
- **문헌(대륙 문헌) 앵커 — 영역 기준으로 개편(2026-06-08)**: 5영역 분할에 맞춰 **번호를 '영역 안 순번'으로** 바꿈(화면 제N권과 일치). 두 형태:
  ① **영역 + 권번호** — `세력 제1권 참고`·`가문 1권 참조`·`세력가문 제1권`·`세력·가문 1권`. 영역 토큰은 **반쪽/붙임/중점 모두 허용**(느슨매칭, `영역찾기`), `제` 생략 가능, `권`/`장` 받음. 그 영역 안에서 n번째(`lore.filter(category)[n-1]`).
  ② **제목** — `(퍼거스 신성 왕국 참고)`. **같은 이름이 여럿이면 전부 주입**(`filter`, 옛 `find` 폐기 — 조용히 버리던 버그 해소).
  - `lib/anchor.mjs`: `parseLoreAnchors`→`{refs:[{area,n}], titles}`(옛 `numbers` 폐기·호출부 `api/story.mjs`·`server` `.refs`로 갱신), `buildLoreAnchorContext`(중복 id 제거, items `{n:영역순번, t}`). 견문록은 짧아 Flash 없이 **그대로 주입**(Gemini 없이 동작·'잠듦' 임시 소환). UI: `volNo`=영역 내 순번(`.ep-no.jang`), 회차 블록보다 앞 system push.
  - ⚠️ **bare `제N권 참고`(영역 없는 번호)는 폐기** — 영역별 번호라 모호. 번호 지목은 반드시 영역 토큰 동반.
- **앵커링 다음 단계 후보**: ①다중 회차 시 '딥리콜'로 최근창 축소(legacy) ②regen에도 앵커 적용 ③지목 회차가 최근창 안이면 중복 생략 ④인물도 이름/번호 앵커.
- **Gemini 일부 연결**: **줄거리 요약**에 첫 사용(`lib/summarize.mjs`, REST fetch, `gemini-2.5-flash`). 아직 미연결: 편지 7종·인물 추출·해시태그.
- **편지/서신 기능**: legacy 7종. Pinyon Script `.script` 서명체가 여기서 진가 발휘.
- **연대기·기억 시스템**: legacy 참고.
- **비용 상한**: Anthropic Console 워크스페이스 spend limit 미설정.
- **본문 문체**: 박제 프롬프트가 Gemini Flash 기준이라 클로드용 톤 재조정 여지. 실제 출력 보고 조정.
- **로딩 애니메이션 + 앵커 확인 자취 — 적용 완료**: `src/Loading.tsx`(점 3개 bounce + 디제틱 문구 4초 페이드 교체, 일반/회상 두 묶음). `…집필 중…` 대체. **회상 톤**: 입력에 앵커 있으면(`src/anchorDetect.ts` 클라 감지) 로딩이 보랏빛·회상 문구로(즉각 확인). **확인 자취**: 서버가 *실제로 되짚은* 회차·문헌을 응답 헤더 `x-recall`(URI 인코딩 JSON)로 보냄 → `anchor.mjs`의 build*Context가 `{block, episodes/items}` 반환하게 변경, story.mjs·server가 본문 전 setHeader. 클라(App)가 그 헤더를 읽어 **토스트**(`.recall-toast`, 위에서 떴다 3.2s 후 사라짐)로 "❧ 되짚은 기록 — 제3화 · 제2권 퍼거스 신성 왕국"을 잠깐 알림(디제틱·시스템어 0, 본문/DB 무관·화면 표시뿐). regen은 앵커 미적용이라 일반 로딩·토스트 없음. 못 찾으면 헤더·토스트 없음(헛걸음 방지).
- **렌더 성능(100화+ 대비) — (a) 간단 윈도잉 적용 완료**: `App.tsx`가 평소엔 최근 `WINDOW`(=30)칸만 렌더, 그 위는 '❧ 이전 두루마리 펼치기' 버튼(`.unfurl`)으로 `STEP`(=30)씩 펼친다. 이야기 새로 불러오면 `loadTurnsFor`가 `visibleCount`를 WINDOW로 리셋(최근 창으로 접음). **인덱스 보존**: `start=max(0,len-visibleCount)`로 잘라 그리되 `i=start+vi`로 원본 번호 유지 → 복사·새로받기·수정·삭제 대상이 안 어긋남. 자동스크롤(끝 ref)·스트리밍·읽기모드와 충돌 없음. ⚠️ 더 필요하면 (b) 진짜 가상 스크롤 `@tanstack/react-virtual`로 승격 — 단 스트리밍·자동스크롤·인라인편집과 상호작용 주의, 브라우저 없는 환경에선 실측 검증 어려움.
- **턴 개별 조작 완료**: 프롬프트/결과 ①수정 ②삭제 ③재생성. 마크다운 렌더·평문 복사 포함.
  · 수정/삭제: `/api/turns` POST·DELETE, App 인라인 편집+낙관적 업데이트.
  · 재생성(③): `api/regen.mjs`·server `/api/regen`(messages+story_id+turn_id) — 매칭 답변만 다시 씀(앞 대화+그 프롬프트만 컨텍스트, 뒤는 그대로). App `새로받기(turnIndex)`가 그 칸만 스트리밍 갱신, 실패 시 원문 복원.
  · 토대: loadTurns가 turn `id` 반환, 생성 후 reload로 id 확보.
- **이야기 복사 완료**: `copyStory(sourceId)`(lib/db) — 새 슬롯에 turns+characters+lore 통째 복사. `/api/stories` POST `{copy_from}` 처리, Stories 패널 행에 '복사' 버튼.

---

## 7. 작업 컨벤션 (이 프로젝트에서)

- 항상 **한국어**로, **비개발자**가 따라오게 설명(용어 풀어서).
- git add/commit/push 자동 처리. 위험한(되돌리기·삭제) 작업만 먼저 확인.
- 디제틱 위장(UI·메시지·코드 주석까지 포드라 안의 단어). UI 언어 사전은 CLAUDE.md 참조. legacy는 읽기 전용 참고.
- 배포는 main 머지 + push. 코드 변경 후 lint+build 통과 확인. 자동배포는 **GitHub Action**(`.github/workflows/deploy.yml`)이 Deploy Hook을 호출해 처리(위 §2). 샌드박스 push가 자동 트리거를 누락하면 **Actions 탭 → Run workflow** 또는 GitHub 웹에서 아무 커밋.
- **뷰/에디트 분리는 앱 전체 공통 원칙**: 모든 콘텐츠 기능(인물·문헌 등)은 읽기 뷰 → '편집'으로 진입. 새 패널 만들 때 이 패턴 따른다.
- 박제 콘텐츠(`worldview/` 4종)는 모델에 직접 박힘 → 표현 하나가 행동에 영향. 수정 후 `npm run bake`+재배포.
